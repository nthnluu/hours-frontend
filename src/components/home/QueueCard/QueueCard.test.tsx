import QueueCard from "./QueueCard";
import {render} from "@testing-library/react";

describe('lorem ipsum...', () => {
    it('should be followed with dolor sit anem', () => {
        // @ts-ignore
        render(<QueueCard foo="asdf" />);
    });
});
