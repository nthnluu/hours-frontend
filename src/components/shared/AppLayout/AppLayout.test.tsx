import AppLayout from "./AppLayout";
import {render} from "@testing-library/react";

describe('lorem ipsum...', () => {
    it('should be followed with dolor sit anem', () => {
        // @ts-ignore
        render(<AppLayout foo="asdf" />);
    });
});
